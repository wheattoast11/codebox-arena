# add

Write a function `add(a, b)` that returns `a + b`.

## signature

```python
def add(a, b):
    ...
```

## verifier

`tests/test_add.py` — must pass under `pytest`.

## difficulty

trivial (sanity-check the active-flavor pipeline)
