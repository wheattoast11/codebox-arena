# count_vowels

Write a function `count_vowels(s)` that returns the number of vowels
(a, e, i, o, u; case-insensitive) in the string `s`.

## signature

```python
def count_vowels(s):
    ...
```

## verifier

`tests/test_count_vowels.py` — must pass under `pytest`.

## difficulty

easy (string iteration + membership)
