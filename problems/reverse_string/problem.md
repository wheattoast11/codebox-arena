# reverse_string

Write a function `reverse_string(s)` that returns the input string
with its characters in reverse order. The empty string reverses to
itself; preserve unicode and whitespace verbatim.

## signature

```python
def reverse_string(s):
    ...
```

## verifier

`tests/test_reverse_string.py` — must pass under `pytest`. Tests check
the empty string, ASCII, repeated characters, whitespace, and a
unicode case.

## difficulty

easy
